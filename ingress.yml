apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    ...
    ...
  labels:
    name: verdaccio
  name: verdaccio
spec:
  rules:
    ...
  tls:
    ...
